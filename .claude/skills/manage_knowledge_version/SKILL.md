---
name: manage_knowledge_version
description: 统一的知识版本管理器，支持版本变更执行、关系维护、依赖分析和溯源管理，提供单条目更新、文档级变更和关系维护三种工作模式
tools: [Read, Write, Edit]
---

# manage_knowledge_version - 知识版本统一管理器

## 🎯 概述

`manage_knowledge_version` 是一个统一的知识版本管理 Skill，整合了版本变更执行和关系维护的完整生命周期。支持从简单的单条目更新到复杂的文档级批量变更，以及专门的版本关系维护，提供一站式版本管理解决方案。

## 🚀 核心功能

### 三种工作模式

1. **单条目更新模式** (`single_entry`)
   - 快速更新单个知识条目的内容和元数据
   - 自动创建版本备份
   - 建立基本的版本映射关系

2. **文档级变更模式** (`document_level`)
   - 管理文档级别的批量版本变更
   - 协调多个相关组件的更新（tags、FAQ、knowledge_points、knowledge_graph）
   - 完整的依赖分析和关系维护
   - 构建版本溯源路径

3. **关系维护模式** (`relationship_only`)
   - 专注于版本关系和依赖图维护
   - 不执行内容更新，仅维护版本映射
   - 适用于批量关系修复和优化场景

### 功能模块

#### 模块1：版本变更执行

- 接收并验证更新内容
- 创建完整的版本备份
- 应用内容更新到知识条目或文档
- 更新版本元数据和变更历史

#### 模块2：关系维护

- 分析知识组件的依赖关系
- 建立新旧版本之间的映射
- 维护完整的版本溯源路径
- 更新依赖关系图和版本索引

#### 模块3：统一验证

- 验证内容更新的一致性
- 检查版本关系的完整性
- 验证依赖关系的正确性
- 生成综合验证报告

## 📋 工作流 SOP

**标准工作流**：

```
1. 接收版本管理请求
   ├─ 识别工作模式（single_entry/document_level/relationship_only）
   ├─ 验证请求参数完整性
   └─ 检查权限和前置条件

2. 版本变更执行阶段（mode ≠ relationship_only）
   ├─ 验证更新权限
   ├─ 创建版本备份
   ├─ 应用内容更新
   ├─ 更新版本元数据
   └─ 记录变更历史

3. 关系维护阶段（mode ≠ single_entry 或按需）
   ├─ 分析组件依赖关系
   ├─ 建立新旧版本映射
   ├─ 维护版本溯源路径
   ├─ 更新依赖关系图
   └─ 更新版本索引

4. 统一验证阶段
   ├─ 验证内容一致性
   ├─ 检查关系完整性
   ├─ 验证依赖正确性
   └─ 生成验证报告

5. 返回完整结果
   ├─ 版本变更状态
   ├─ 版本映射关系
   ├─ 溯源路径
   ├─ 依赖关系图
   └─ 验证结果
```

### 详细流程说明

**步骤1：接收版本管理请求**

- 解析工作模式参数（single_entry/document_level/relationship_only）
- 验证必需参数的完整性和格式
- 检查知识ID或文档ID的有效性
- 验证用户权限和操作合法性

**步骤2：版本变更执行阶段**

- **权限验证**：检查更新权限，验证知识条目锁定状态
- **备份创建**：创建当前版本的完整备份，记录备份位置和时间戳
- **内容更新**：应用更新内容，处理冲突和合并，更新相关字段
- **元数据管理**：生成新版本号，更新版本元数据，记录变更摘要
- **历史记录**：创建详细的变更记录，关联变更原因和说明

**步骤3：关系维护阶段**

- **依赖分析**：分析知识组件间的依赖关系，识别关键依赖和引用
- **版本映射**：为每个组件创建版本映射，确定映射类型（direct/structural/content）
- **溯源路径**：构建完整的历史追溯路径，维护版本间的派生关系
- **关系图更新**：更新依赖关系图，检测循环依赖和冲突
- **索引维护**：更新版本索引数据库，优化查询性能

**步骤4：统一验证阶段**

- **内容一致性**：检查更新后的内容完整性，验证格式正确性
- **关系完整性**：验证所有映射的完整性，检查溯源路径的连续性
- **依赖正确性**：验证依赖关系的正确性，检查引用关系的有效性
- **综合报告**：生成完整的验证报告，标记潜在问题

**步骤5：返回完整结果**

- 返回版本变更状态和新版本号
- 提供版本映射关系和溯源路径
- 返回依赖关系图和验证结果
- 提供备份位置和回滚信息

## 📋 输入规范

### 必需输入

```json
{
  "mode": "document_level",  // single_entry | document_level | relationship_only
  
  // 版本变更信息（mode = single_entry 或 document_level 时必需）
  "version_change": {
    "knowledge_id": "knl_001",
    "updated_content": {
      "title": "员工年假管理规定（2024年修订）",
      "content": "更新后的政策内容..."
    },
    "change_reason": "根据最新劳动法调整年假政策",
    "change_summary": "更新年假计算规则，增加特殊情况处理"
  },
  
  // 关系维护信息（mode = document_level 或 relationship_only 时必需）
  "relationship_maintenance": {
    "version_changes": [
      {
        "component_type": "knowledge_entry",
        "old_version_id": "knl_001_v1",
        "new_version_id": "knl_001_v2",
        "change_type": "content_update"
      },
      {
        "component_type": "tags",
        "old_version_id": "tags_001_v1",
        "new_version_id": "tags_001_v2",
        "change_type": "structural_update"
      }
    ],
    "component_dependencies": {
      "knl_001": ["tags_001", "faq_001", "knowledge_points_001"],
      "tags_001": ["knowledge_graph_001"]
    }
  }
}
```

### 可选输入

```json
{
  "options": {
    // 备份选项
    "create_backup": true,
    "backup_location": "/backups",
    
    // 关系维护选项
    "maintain_full_history": true,
    "enable_rollback": true,
    "track_dependencies": true,
    
    // 验证选项
    "check_circular_deps": true,
    "verify_completeness": true,
    
    // 影响评估
    "impact_assessment": {
      "affected_components": ["tags", "faq", "knowledge_points", "knowledge_graph"],
      "estimated_effort": "4小时"
    }
  }
}
```

## 📤 输出内容

### 标准输出（完整模式）

```json
{
  // 版本变更结果
  "version_update": {
    "status": "success",
    "knowledge_id": "knl_001",
    "previous_version": "v1.0",
    "new_version": "v2.0",
    "update_time": "2024-01-20T10:30:00Z",
    "backup_location": "/backups/knl_001_v1_20240120_103000",
    "change_history": {
      "change_id": "chg_001",
      "changes_made": ["更新年假计算规则", "增加特殊情况处理"]
    }
  },
  
  // 组件更新详情（document_level 模式）
  "updated_components": {
    "tags": {
      "status": "updated",
      "changes": ["新增标签：特殊情况处理", "修改标签：计算规则"],
      "processing_time": 15
    },
    "faq": {
      "status": "updated",
      "changes": ["新增FAQ：特殊情况处理", "修改FAQ：计算规则"],
      "processing_time": 25
    },
    "knowledge_points": {
      "status": "updated",
      "changes": ["新增知识点：特殊情况", "修改知识点：计算方法"],
      "processing_time": 20
    },
    "knowledge_graph": {
      "status": "updated",
      "changes": ["更新实体关系", "添加新实体"],
      "processing_time": 30
    }
  },
  
  // 版本映射关系
  "version_mappings": [
    {
      "component_type": "knowledge_entry",
      "old_version": "knl_001_v1",
      "new_version": "knl_001_v2",
      "mapping_type": "direct",
      "confidence": 0.98,
      "change_summary": {
        "fields_changed": ["content", "tags"],
        "change_percentage": 0.15
      }
    },
    {
      "component_type": "tags",
      "old_version": "tags_001_v1",
      "new_version": "tags_001_v2",
      "mapping_type": "structural",
      "confidence": 0.92,
      "change_summary": {
        "tags_added": 2,
        "tags_removed": 1,
        "tags_modified": 3
      }
    }
  ],
  
  // 溯源路径
  "traceability_paths": [
    {
      "knowledge_id": "knl_001",
      "version_history": ["v1.0", "v1.1", "v2.0"],
      "current_version": "v2.0",
      "creation_date": "2023-01-15",
      "last_updated": "2024-01-20",
      "derivation_path": "v1.0 → v1.1 → v2.0"
    }
  ],
  
  // 依赖关系图
  "dependency_graph": {
    "nodes": [
      {"id": "knl_001_v2", "type": "knowledge_entry", "version": "v2.0"},
      {"id": "tags_001_v2", "type": "tags", "version": "v2.0"},
      {"id": "faq_001_v2", "type": "faq", "version": "v2.0"}
    ],
    "edges": [
      {"from": "knl_001_v2", "to": "tags_001_v2", "type": "uses", "strength": 0.95},
      {"from": "knl_001_v2", "to": "faq_001_v2", "type": "references", "strength": 0.88}
    ]
  },
  
  // 统一验证结果
  "validation_results": {
    "content_consistency": {
      "status": "passed",
      "checks_performed": 8,
      "issues_found": 0
    },
    "relationship_integrity": {
      "status": "passed",
      "mappings_verified": 8,
      "paths_verified": 5
    },
    "dependency_correctness": {
      "status": "passed",
      "dependencies_verified": 12,
      "circular_deps_found": 0
    },
    "overall_status": "passed",
    "validation_time": 5.2
  },
  
  // 处理统计
  "processing_stats": {
    "total_time": 92.5,
    "mappings_created": 8,
    "paths_maintained": 5,
    "dependencies_verified": 12,
    "components_updated": 4
  }
}
```

## 🎪 使用示例

### 示例1：单条目快速更新

```
使用 manage_knowledge_version 更新年假政策内容

工作模式：single_entry
知识ID：knl_001
更新内容：新的年假计算规则
变更原因：根据最新劳动法调整年假政策

处理过程：
1. 接收更新请求
2. 验证更新权限 ✓
3. 创建版本备份：/backups/knl_001_v1_20240120
4. 应用内容更新
5. 更新版本：v1.0 → v2.0
6. 建立基本版本映射
7. 验证内容一致性 ✓

结果：
- 更新状态：成功
- 新版本：v2.0
- 备份位置：/backups/knl_001_v1_20240120
- 处理时间：2.3秒
```

### 示例2：文档级批量变更

```
使用 manage_knowledge_version 管理年假政策文档级版本变更

工作模式：document_level
变更文档：年假政策 v2.0
前一版本：v1.0
变更描述：更新年假计算规则，增加特殊情况处理
影响组件：tags、FAQ、knowledge_points、knowledge_graph

处理过程：
1. 接收文档级变更请求
2. 版本变更执行阶段：
   - 验证权限 ✓
   - 创建备份 ✓
   - 更新文档内容 ✓
   - 更新4个关联组件 ✓
   - 生成新版本号：v2.0

3. 关系维护阶段：
   - 分析依赖关系：12个依赖
   - 建立版本映射：
     * 知识条目：v1.0 → v2.0（直接映射，置信度0.98）
     * 标签：v1.0 → v2.0（结构映射，置信度0.92）
     * FAQ：v1.0 → v2.0（内容映射，置信度0.95）
     * 知识点：v1.0 → v2.0（直接映射，置信度0.97）
   - 维护溯源路径：v1.0 → v1.1 → v2.0
   - 更新依赖关系图 ✓

4. 统一验证阶段：
   - 内容一致性：通过（8项检查）
   - 关系完整性：通过（8个映射，5条路径）
   - 依赖正确性：通过（12个依赖，0个循环）

结果：
- 更新状态：成功
- 新版本：v2.0
- 组件更新：4个（tags、FAQ、knowledge_points、knowledge_graph）
- 版本映射：8个
- 溯源路径：5条
- 依赖验证：12个
- 总处理时间：92.5秒
```

### 示例3：纯关系维护

```
使用 manage_knowledge_version 维护版本关系

工作模式：relationship_only
场景：批量修复版本映射关系

版本变更集合：
- 知识条目：knl_001_v1 → knl_001_v2
- 标签：tags_001_v1 → tags_001_v2
- FAQ：faq_001_v1 → faq_001_v2

依赖关系：
- knl_001 依赖 tags_001、faq_001
- tags_001 依赖 knowledge_graph_001

处理过程：
1. 接收关系维护请求
2. 跳过内容更新阶段
3. 关系维护阶段：
   - 分析依赖关系 ✓
   - 建立版本映射（3个组件）
   - 维护溯源路径
   - 更新依赖关系图
   - 更新版本索引

4. 验证阶段：
   - 关系完整性：通过
   - 依赖正确性：通过

结果：
- 维护状态：成功
- 映射创建：3个
- 路径维护：3条
- 依赖验证：5个
- 处理时间：2.8秒
```

## 🔧 配置选项

### 基础配置

```yaml
version_management:
  # 默认工作模式
  default_mode: "single_entry"
  
  # 备份配置
  backup:
    enabled: true
    location: "/backups"
    retention_days: 90
    
  # 版本号生成策略
  versioning:
    strategy: "semantic"  # semantic | timestamp | incremental
    auto_increment: true
```

### 关系维护配置

```yaml
relationship_maintenance:
  # 映射配置
  mapping:
    create_direct_mappings: true
    create_structural_mappings: true
    confidence_threshold: 0.9
    
  # 溯源配置
  traceability:
    maintain_full_history: true
    enable_version_rollback: true
    track_all_changes: true
    max_history_depth: 100
    
  # 依赖分析
  dependency:
    analyze_dependencies: true
    verify_completeness: true
    check_circular_refs: true
    max_dependency_depth: 10
```

### 验证配置

```yaml
validation:
  # 内容验证
  content:
    check_format: true
    check_completeness: true
    check_consistency: true
    
  # 关系验证
  relationship:
    verify_mappings: true
    verify_paths: true
    check_integrity: true
    
  # 依赖验证
  dependency:
    verify_correctness: true
    detect_conflicts: true
    check_circular_deps: true
```

### 高级配置

```yaml
advanced:
  # 性能优化
  performance:
    enable_caching: true
    cache_ttl: 3600
    batch_size: 100
    parallel_processing: true
    
  # 索引管理
  indexing:
    update_version_index: true
    optimize_query_performance: true
    enable_full_text_search: true
    rebuild_threshold: 1000
    
  # 图维护
  graph_maintenance:
    update_dependency_graph: true
    optimize_graph_layout: true
    detect_orphan_nodes: true
    prune_old_versions: false
```

## ⚠️ 注意事项

### 模式选择

- **单条目更新**：适用于简单的内容修改，快速高效
- **文档级变更**：适用于复杂的批量更新，需要完整的关系维护
- **关系维护**：适用于修复或优化版本关系，不涉及内容更新

### 数据完整性

- **备份策略**：确保每次更新前创建完整备份
- **映射准确性**：验证版本映射的置信度，低于阈值时发出警告
- **路径完整性**：维护完整的溯源路径，避免路径断裂
- **依赖一致性**：保持依赖关系的一致性，检测循环依赖

### 性能优化

- **增量维护**：只维护变更的部分，避免全量更新
- **批量处理**：支持批量版本变更，提高处理效率
- **异步处理**：大量维护时使用异步处理，避免阻塞
- **缓存策略**：缓存常用的版本映射和依赖关系

### 错误处理

- **权限错误**：验证用户权限，拒绝未授权操作
- **映射冲突**：检测并处理版本映射冲突
- **依赖缺失**：处理依赖关系缺失的情况
- **路径断裂**：修复溯源路径断裂问题
- **回滚机制**：提供完整的回滚能力

## 📊 质量验证指标

### 核心指标

- **版本更新成功率**: ≥98%（目标值）
- **版本管理准确率**: 100%（目标值）
- **备份成功率**: 100%（目标值）
- **关联关系准确率**: ≥98%（目标值）
- **溯源路径完整性**: ≥95%（目标值）
- **依赖验证准确率**: ≥95%（目标值）
- **一致性验证通过率**: ≥95%（目标值）
- **端到端成功率**: ≥95%（目标值）

### 评估维度

- **准确性**: 版本变更和关系维护的准确性
- **完整性**: 备份、映射、路径的完整性
- **一致性**: 内容、关系、依赖的一致性
- **性能**: 处理速度和资源消耗
- **可靠性**: 错误处理和恢复能力

## 🔄 与其他 Skills 的协作

### 前置 Skills

- `validate_knowledge_quality`: 验证更新内容的质量
- `search_knowledge_base`: 查找需要更新的知识条目

### 后续 Skills

- `retire_obsolete_knowledge`: 退役被替代的旧版本
- `analyze_knowledge_usage`: 分析版本使用情况

### 协作示例

```
完整的知识更新流程：
1. search_knowledge_base - 查找需要更新的知识
2. validate_knowledge_quality - 验证新内容质量
3. manage_knowledge_version - 执行版本更新和关系维护
4. analyze_knowledge_usage - 分析更新后的使用情况
5. retire_obsolete_knowledge - 退役旧版本（如需要）
```

---

**manage_knowledge_version** - 统一版本管理，确保知识演进可追溯！ 🔄✨
