# 天气API集成示例

## 📋 示例概述

这个示例展示了如何使用 `weather_api_integrator` Skill集成天气预报API，获取实时天气信息并提供智能天气服务。

## 🚀 快速开始

### 基本使用
```
使用 weather_api_integrator 获取当前天气
城市: 北京
输出格式: 简洁文本
```

### 高级使用
```
使用 weather_api_integrator 获取详细天气预报
位置: 北京市朝阳区
天数: 7天预报
包含: 温度、湿度、风速、降水概率
输出格式: 结构化JSON
```

## 🌤️ API集成配置

### API端点
- **基础URL**: `https://api.weather.com`
- **当前天气**: `/current`
- **天气预报**: `/forecast`
- **历史数据**: `/historical`

### 认证方式
- **类型**: API密钥认证
- **参数**: `api_key` 参数
- **安全**: HTTPS加密传输

### 请求示例
```python
# 获取当前天气
GET https://api.weather.com/current?city=Beijing&api_key=YOUR_API_KEY

# 获取天气预报
GET https://api.weather.com/forecast?city=Beijing&days=7&api_key=YOUR_API_KEY
```

## 📊 数据格式

### 请求参数
```json
{
  "city": "Beijing",
  "country": "CN",
  "units": "metric",
  "lang": "zh-CN"
}
```

### 响应格式
```json
{
  "location": {
    "city": "Beijing",
    "country": "China",
    "latitude": 39.9042,
    "longitude": 116.4074
  },
  "current": {
    "temperature": 25,
    "humidity": 65,
    "wind_speed": 3.5,
    "description": "晴朗",
    "icon": "01d"
  },
  "forecast": [
    {
      "date": "2024-01-15",
      "high": 28,
      "low": 18,
      "description": "多云"
    }
  ]
}
```

## 🔧 功能特性

### 实时天气查询
- 当前温度、湿度、风速
- 天气状况描述
- 日出日落时间
- 紫外线指数

### 天气预报
- 多日天气预报（1-7天）
- 最高/最低温度
- 降水概率
- 风向风力

### 地理位置支持
- 城市名称搜索
- 经纬度坐标
- IP地址定位
- 模糊匹配

### 缓存和性能
- 响应缓存（默认5分钟）
- 请求限流控制
- 错误重试机制
- 连接池管理

## 🎯 使用场景

### 场景1: 出行规划
```
使用 weather_api_integrator 获取目的地天气
目的地: 上海
行程: 3天行程
需求: 温度范围、降水概率、穿衣建议
输出: 出行天气建议报告
```

### 场景2: 农业管理
```
使用 weather_api_integrator 监控农田天气
位置: 农田坐标
监测: 温度变化、降雨量、湿度
预警: 极端天气警报
输出: 农业天气监测报告
```

### 场景3: 活动策划
```
使用 weather_api_integrator 评估活动天气
活动日期: 2024-06-15
地点: 室外体育场
备份方案: 室内场所天气对比
输出: 活动天气风险评估
```

## ⚙️ 配置选项

### API配置
```yaml
api:
  base_url: https://api.weather.com
  timeout: 30
  retry_attempts: 3
  cache_ttl: 300  # 5分钟缓存
```

### 地理位置配置
```yaml
location:
  default_city: "Beijing"
  default_country: "CN"
  units: "metric"  # metric/imperial
  language: "zh-CN"
```

### 显示配置
```yaml
display:
  temperature_unit: "°C"
  wind_unit: "m/s"
  date_format: "YYYY-MM-DD"
  include_icons: true
```

## 🔄 高级功能

### 批量查询
```python
# 批量获取多个城市天气
cities = ["Beijing", "Shanghai", "Guangzhou", "Shenzhen"]
for city in cities:
    weather = get_weather(city)
    # 处理天气数据
```

### 历史数据对比
```python
# 对比历史天气数据
today = get_weather("Beijing")
last_year = get_historical_weather("Beijing", "2023-01-15")
comparison = compare_weather(today, last_year)
```

### 天气预警系统
```python
# 设置天气预警阈值
alerts = check_weather_alerts("Beijing", {
    "high_temperature": 35,
    "low_temperature": -10,
    "heavy_rain": 50  # mm
})
```

## 🛡️ 错误处理

### 常见错误
- **网络错误**: 自动重试机制
- **认证失败**: 提示更新API密钥
- **位置无效**: 提供位置建议
- **数据缺失**: 优雅降级处理

### 错误响应示例
```json
{
  "error": {
    "code": 401,
    "message": "认证失败，请检查API密钥",
    "suggestion": "请确认API密钥是否正确配置"
  }
}
```

## 📈 性能优化

### 缓存策略
- **内存缓存**: 频繁查询结果缓存
- **磁盘缓存**: 历史数据持久化
- **CDN缓存**: 静态资源缓存

### 请求优化
- **连接复用**: HTTP Keep-Alive
- **压缩传输**: Gzip压缩
- **批量请求**: 减少API调用次数

## 🎪 示例输出

### 简洁文本输出
```
🌤️ 北京天气报告
当前温度: 25°C | 湿度: 65% | 风速: 3.5m/s
天气状况: 晴朗
明日预报: 26°C/18°C 多云
```

### 详细JSON输出
```json
{
  "location": "北京, 中国",
  "current": {
    "temperature": 25,
    "feels_like": 26,
    "humidity": 65,
    "pressure": 1013,
    "wind_speed": 3.5,
    "wind_direction": "东北风",
    "description": "晴朗",
    "visibility": 10,
    "uv_index": 6
  },
  "forecast": [
    {
      "date": "2024-01-16",
      "high": 26,
      "low": 18,
      "description": "多云",
      "precipitation": 20
    }
  ]
}
```

## ⚠️ 注意事项

### 使用限制
- API调用频率限制（通常60次/分钟）
- 数据更新频率（通常15-30分钟）
- 免费版功能限制

### 数据准确性
- 天气预测存在不确定性
- 实时数据可能有延迟
- 不同数据源可能不一致

### 隐私保护
- 不存储用户位置历史
- 加密传输敏感信息
- 遵守数据隐私法规

## 📞 技术支持

### 故障排查
1. **检查网络连接**
2. **验证API密钥**
3. **查看错误日志**
4. **测试API端点**

### 获取帮助
- 查看API文档了解详细参数
- 参考错误代码说明
- 联系技术支持团队

---

**这个示例展示了天气API集成Skill的强大功能，您可以根据具体需求定制天气服务和扩展功能。** 🌈